spring:
  cloud:
    config:
    discovery:
      enabled: true
      profile: prod
      uri:
        - http://config-server:8888
      fail-fast: true
  application:
    name: clevertec-final-project